version: 0.0
os: linux
files:
  - source: MessageAPI.war
    destination: /opt/tomcat/webapps/
  - source: FileReader.jar
    destination: /opt/pipeline/FileReader
  - source: DataReader.jar
    destination: /opt/pipeline/DataReader
hooks:
  BeforeInstall:
    - location: cleanup.sh
      timeout: 300
      runas: root
