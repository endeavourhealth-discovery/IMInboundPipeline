import "/EMIS/common/MobileTelephoneNumber.jslt" as mobiletel
import "/Utilities.jslt" as utils

let MobileTelId = newUuid()

{
    "entities" : utils:cleanArray([
        mobiletel:EMISMobileTelephoneNumberTransform(., $MobileTelId),
    ])
}